name: Build and Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      # - name: Setup Node.js
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: 14.x

      # - name: Install NPM packages
      #   working-directory: ./backend
      #   run: |
      #     npm i -g yarn
      #     yarn install

      # - name: Frontend build
      #   working-directory: ./backend
      #   run: yarn production

      - name: SSH key generate
        run: echo "$SSH_PRIVATE_KEY" > laract-chat_rsa && chmod 600 laract-chat_rsa
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: SSH key test
        working-directory: ./backend/public
        run: echo /home/runner/work/laract-chat/laract-chat/laract-chat_rsa

      # - name: Frontend deploy
      #   working-directory: ./backend/public
      #   run: rsync -auzrv -e "ssh -i /home/runner/work/laract-chat/laract-chat/laract-chat_rsa -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -p ${{secrets.SSH_PORT}}" --delete ./* ${{secrets.SERVER_USERNAME}}@${{secrets.SERVER_HOST}}:${{secrets.SERVER_DEPLOY_DIR}}public/
